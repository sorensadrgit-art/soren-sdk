name: Phase 8 Review Trigger

on:
  pull_request:
    paths:
      - "packages/planner/**"
      - "packages/verification/**"
      - "packages/evidence/**"
      - ".github/workflows/phase8-review-ci-trigger.yml"

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Permanent Contracts CI is the authoritative gate."
