{
  "name": "@soren-sdk/sdk",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript SDK clients for Soren SDK protocol surfaces.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @soren-sdk/contracts build && pnpm --filter @soren-sdk/core build && pnpm --filter @soren-sdk/connectors build && pnpm --filter @soren-sdk/application build && pnpm --filter @soren-sdk/protocol-server build && vitest run"
  },
  "dependencies": {
    "@soren-sdk/application": "workspace:*",
    "@soren-sdk/contracts": "workspace:*"
  },
  "devDependencies": {
    "@soren-sdk/protocol-server": "workspace:*",
    "@types/node": "24.13.3",
    "typescript": "6.0.3",
    "vitest": "4.1.10"
  }
}
